services:
  # Redis para distribución de mensajes
  # NOTA: Ahora usando Upstash en la nube, servicio local desactivado
  # redis:
  #   image: redis:7-alpine
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   command: redis-server --appendonly yes

# volumes:
#   redis_data:
